# 운빨 아발론 v5.1 — 2차 개선 기획서
> Gemini Canvas 직접 구현용 | 코드 레벨 명세서
> 기준 파일: game.js (1602줄) + index.html

---

## 📋 개선 항목 요약

| # | 항목 | 원인 | 조치 |
|---|------|------|------|
| 1 | 모바일 HUD 엉망 | 스킬바 배경박스·BGM버튼·골드표시 등 | HTML+JS 구조 전면 재설계 |
| 2 | 사운드 지지지 노이즈 | 全 엔티티 공격마다 playSFX 호출 | 플레이어 관련 타격만 소리, BGM 삭제 |
| 3 | 미니맵 실시간 반영 불량 | gameTimerMini 미업데이트, 죽은 엔티티 표시 | 필터·업데이트 수정 |
| 4 | 타워 극초반 붕괴 + 넥서스 무방비 | HP낮음, 방어 순서 없음 | HP↑ + 수호탑 다 깨져야 넥서스 피해 |
| 5 | 레벨업 너무 느림 | EXP 획득량 부족 | EXP 전반 상향 |
| 6 | 경기종료 통계 없음 | 미구현 | 종합 킬/뎃/딜 현황표 추가 |

---

## 1. 모바일 HUD 전면 재설계

### 1-1. index.html — BGM 버튼 완전 제거

아래 HTML 블록 **전체 삭제** (BGM 버튼):
```html
<!-- 삭제할 블록 -->
<button onclick="playNextBGM()" id="btnManualBGM"
        class="bg-gradient-to-r from-slate-700 ...">
    <span class="hidden md:inline">🎵 BGM 켜기/변경</span>
    <span class="md:hidden">🎵 BGM</span>
</button>
```

아래 iframe도 **삭제**:
```html
<!-- 삭제 -->
<iframe id="bgmFallback" class="hidden" allow="autoplay"></iframe>
```

### 1-2. index.html — 우상단 골드 표시 강화

기존 상점 버튼 영역:
```html
<!-- 기존 -->
<button onclick="toggleShop()" class="bg-gradient-to-r from-amber-500 to-yellow-400 ...">
    <span>🛒 상점</span>
    <span id="hudGoldText" class="bg-slate-950 text-amber-400 font-mono px-1.5 py-0.5 rounded text-[9px]">0G</span>
</button>
```
를 아래로 교체 (골드 표시를 위쪽 독립 라인으로 분리):
```html
<!-- 교체 -->
<div class="flex flex-col items-end gap-1">
    <!-- 골드 표시 (항상 보임) -->
    <div class="bg-slate-950/95 border border-amber-500/50 rounded-lg px-2 py-1 flex items-center gap-1.5">
        <span class="text-amber-400 text-xs">💰</span>
        <span id="hudGoldText" class="text-amber-300 font-black font-mono text-sm">0G</span>
    </div>
    <!-- 상점 버튼 -->
    <button onclick="toggleShop()"
            class="bg-gradient-to-r from-amber-500 to-yellow-400 text-slate-950 font-black text-[11px] px-3 py-2 rounded-xl shadow-lg flex items-center gap-1 border border-amber-300 active:scale-95 transition-transform">
        🛒 상점
    </button>
</div>
```

### 1-3. index.html — 우하단 스킬바 완전 재설계

**기존 `#uiSkillBar` 전체 블록 교체:**

```html
<!-- 기존 전체 삭제하고 아래로 교체 -->
<!-- 패시브 스킬 아이콘 바 (하단 중앙, 모바일에서 작게) -->
<div id="passiveSkillBar" class="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-1 pointer-events-none z-10"></div>

<!-- 스킬 버튼 2개 (우하단 플로팅, 배경 없이) -->
<div id="uiSkillBar" class="pointer-events-auto absolute bottom-4 right-4 flex flex-col items-center gap-2 z-20">
    
    <!-- AUTO 토글 (아주 작은 pill) -->
    <button onmousedown="toggleAutoSkill()" ontouchstart="toggleAutoSkill(); event.preventDefault();"
            id="btnAutoSkill"
            class="text-[9px] font-black px-2 py-0.5 rounded-full border transition-all active:scale-90"
            style="background:rgba(245,158,11,0.85); color:#0f172a; border-color:rgba(252,211,77,0.6);">
        <span id="txtAutoSkill">AUTO</span>
    </button>

    <!-- 스킬 2 버튼 (배경 반투명 원형) -->
    <button onmousedown="triggerSkill(2)" ontouchstart="triggerSkill(2); event.preventDefault();"
            class="relative overflow-hidden flex flex-col items-center justify-center active:scale-90 transition-transform"
            style="width:60px; height:60px; border-radius:50%; background:rgba(99,102,241,0.3); border:2px solid rgba(99,102,241,0.7); backdrop-filter:blur(4px);">
        <span id="txtSkill2Icon" class="text-2xl leading-none">💫</span>
        <div id="maskSkill2" class="hidden absolute inset-0 rounded-full flex items-center justify-center font-mono font-black text-amber-400 text-xs" style="background:rgba(15,23,42,0.75);"></div>
    </button>

    <!-- 스킬 1 버튼 -->
    <button onmousedown="triggerSkill(1)" ontouchstart="triggerSkill(1); event.preventDefault();"
            class="relative overflow-hidden flex flex-col items-center justify-center active:scale-90 transition-transform"
            style="width:60px; height:60px; border-radius:50%; background:rgba(16,185,129,0.3); border:2px solid rgba(16,185,129,0.7); backdrop-filter:blur(4px);">
        <span id="txtSkill1Icon" class="text-2xl leading-none">✨</span>
        <div id="maskSkill1" class="hidden absolute inset-0 rounded-full flex items-center justify-center font-mono font-black text-amber-400 text-xs" style="background:rgba(15,23,42,0.75);"></div>
    </button>
</div>
```

> ⚠️ **주의**: `#passiveSkillBar`가 이동되었으므로 `updateUI()` 내 passiveSkillBar 업데이트 코드는 그대로 유지.

### 1-4. index.html — 상점 패널 모바일 위치 조정

기존: `class="absolute right-2 top-20 z-40 ... w-64 md:w-72"`  
교체: 모바일에서 전체화면 슬라이드업 형태로:
```html
<div id="shopUI" class="absolute right-0 bottom-0 md:right-2 md:top-20 md:bottom-auto z-40 
     bg-slate-950/98 border border-slate-700 rounded-t-2xl md:rounded-2xl p-3 
     w-full md:w-72 text-white hidden pointer-events-auto shadow-2xl backdrop-blur-xl"
     style="max-height: 60vh; display:none;">
    <div class="flex justify-between items-center border-b border-slate-800 pb-2 mb-2">
        <h2 class="text-sm font-black text-amber-400">🛒 보급소</h2>
        <div class="text-[8px] text-slate-400 hidden md:block">중복구매 = 강화 (+1~+9)</div>
        <button onclick="toggleShop()" class="text-slate-400 text-xl font-bold ml-2">&times;</button>
    </div>
    <div class="flex flex-col gap-1.5 overflow-y-auto pr-0.5" style="max-height:calc(60vh - 60px);" id="shopItemContainer"></div>
</div>
```

`toggleShop()` 함수 내에서 `classList.toggle('hidden')` 대신:
```javascript
window.toggleShop = () => {
    const shop = document.getElementById('shopUI');
    const isHidden = shop.style.display === 'none' || shop.style.display === '';
    shop.style.display = isHidden ? 'block' : 'none';
    if (isHidden) renderShop();
};
```

### 1-5. index.html — 미니맵 좌측 고정 (두번째 줄 제거)

현재 미니맵이 HUD 두번째 줄에 `justify-start`로 배치되어 좌상단 플레이어 정보 아래에 독립 row로 있는 구조. 이게 모바일에서 공간 낭비 + 레이아웃 밀림 원인.

**미니맵 블록을 좌상단 플레이어 정보 카드 아래에 직접 붙이기:**

`<!-- 두번째 줄: 미니맵 -->` 전체 블록(div 포함) 삭제하고,  
좌상단 `flex flex-col gap-1` div 내부 (캐릭터 정보 카드 아래)에 직접 삽입:

```html
<!-- 캐릭터 정보 카드 바로 아래에 추가 -->
<div class="bg-slate-950/90 border border-slate-800 rounded-xl p-1 shadow-xl backdrop-blur-md" style="width:80px;">
    <div class="flex justify-between items-center mb-0.5 px-0.5">
        <span class="text-[6px] text-slate-500 font-bold uppercase">MAP</span>
        <span id="gameTimerMini" class="text-[6px] text-amber-400 font-mono font-bold">00:00</span>
    </div>
    <div class="relative w-full overflow-hidden rounded" style="height:80px; background:#0f172a;">
        <canvas id="minimapCanvas" style="width:100%; height:100%;"></canvas>
    </div>
</div>
```

---

## 2. 사운드 시스템 수정

### 2-1. game.js — BGM 관련 코드 전체 삭제

아래 전역 변수 및 함수들 **완전 삭제**:
```javascript
// 삭제: const BGM_URLS = [...];
// 삭제: let currentBgmIndex = 0;
// 삭제: let bgmAudio = new Audio();
// 삭제: bgmAudio.volume = 0.3;
// 삭제: function playNextBGM() { ... }
```

`initAudio()` 함수에서 `playNextBGM()` 호출 제거:
```javascript
// 기존 initAudio
function initAudio() {
    try {
        if(!audioCtx) {
            let AudioContext = window.AudioContext || window.webkitAudioContext;
            if(AudioContext) {
                audioCtx = new AudioContext();
                playNextBGM(); // ← 이 줄 삭제
            }
        } else if(audioCtx.state === 'suspended') {
            audioCtx.resume();
        }
    } catch(e) {
        console.warn('Audio Context initialization failed:', e);
    }
}
```

### 2-2. game.js — 사운드 발생 범위 제한

**핵심 문제**: `applyRawDamage()` 내 `playSFX('hit')`이 모든 전투(AI vs AI 포함)에서 호출되어 초당 수십회 노이즈 발생.

`applyRawDamage()` 맨 마지막의 `playSFX('hit');` 를 아래로 교체:
```javascript
// 기존 (맨 마지막 줄):
playSFX('hit');

// 교체: 플레이어가 공격하거나 플레이어가 맞을 때만 소리
if (attacker === player || this === player) {
    playSFX(attacker === player ? 'hit' : 'hit_receive');
}
```

`autoAttack()` 내 원거리 공격:
```javascript
// 기존: playSFX('shoot');
// 교체:
if (this.isPlayer) playSFX('shoot');
```

타워 공격 (`Building.update` 내):
```javascript
// 기존: playSFX('tower');
// 교체:
if (this.faction === player?.faction) playSFX('tower');
```

미니언 공격 (`Minion.update` 내):
```javascript
// 기존: playSFX('hit');
// 교체: 삭제 (미니언 타격음 제거)
```

### 2-3. game.js — 사운드 품질 개선

기존 `playSFX` 함수의 `hit` 타입과 새로운 `hit_receive` 타입 수정:

```javascript
if (type === 'hit') {
    // 플레이어가 때릴 때: 묵직하고 짧은 타격음
    master.gain.setValueAtTime(0.45, now);
    play('osc', 180, 60, 'sine', 0.9, 0.004, 0.07);
    playNoise(0.5, 600, 0.003, 0.055, 'bandpass');

} else if (type === 'hit_receive') {
    // 플레이어가 맞을 때: 약간 다른 느낌 (높은 톤)
    master.gain.setValueAtTime(0.35, now);
    play('osc', 250, 80, 'triangle', 0.7, 0.003, 0.08);
    playNoise(0.4, 1200, 0.002, 0.06, 'bandpass');
```

> **참고**: `createNoiseBuffer`가 매 호출마다 새 버퍼를 생성하는 게 성능 이슈. 아래 캐싱 추가:
```javascript
// 전역에 추가:
let _cachedNoiseBuffer = null;
function createNoiseBuffer(ctx, duration) {
    // 0.3초 이하는 캐시된 버퍼 재사용 (음색 동일, 성능 향상)
    if (duration <= 0.3 && _cachedNoiseBuffer) return _cachedNoiseBuffer;
    const sampleRate = ctx.sampleRate;
    const buf = ctx.createBuffer(1, Math.ceil(sampleRate * duration), sampleRate);
    const data = buf.getChannelData(0);
    for (let i = 0; i < data.length; i++) data[i] = Math.random() * 2 - 1;
    if (duration <= 0.3) _cachedNoiseBuffer = buf;
    return buf;
}
```

---

## 3. 미니맵 실시간 반영 수정

### 3-1. game.js — gameTimerMini 업데이트 누락 수정

`updateUI()` 함수 내 타이머 업데이트 부분에 추가:
```javascript
// 기존:
document.getElementById('gameTimer').textContent = m.toString().padStart(2,'0')+':'+s.toString().padStart(2,'0');

// 추가 (바로 다음 줄):
const timerMini = document.getElementById('gameTimerMini');
if (timerMini) timerMini.textContent = m.toString().padStart(2,'0')+':'+s.toString().padStart(2,'0');
```

### 3-2. game.js — drawMinimap 실시간 렌더 확인

기존 `drawMinimap()` 에서 `mCanvas.width = mCanvas.clientWidth` 매 프레임 재설정이 깜빡임 원인. 수정:
```javascript
function drawMinimap() {
    // 캔버스 크기 한 번만 설정 (변경 없으면 스킵)
    if (mCanvas.width !== mCanvas.clientWidth || mCanvas.height !== mCanvas.clientHeight) {
        mCanvas.width = mCanvas.clientWidth || 80;
        mCanvas.height = mCanvas.clientHeight || 80;
    }
    mCtx.fillStyle = '#0f172a';
    mCtx.fillRect(0, 0, mCanvas.width, mCanvas.height);
    
    const sx = mCanvas.width / MAP_SIZE, sy = mCanvas.height / MAP_SIZE;
    
    // 라인 그리기 (기존 동일)
    mCtx.fillStyle = 'rgba(139,90,43,0.4)';
    // ... (기존 라인 코드 유지)
    
    // 엔티티 렌더 - 조건 수정: 죽은 영웅은 미니맵에서 제거
    entities.forEach(e => {
        if (e.isDead) return; // 모든 죽은 엔티티 제외 (기존: jungle만 예외)
        
        let col = e.faction==='BLUE' ? '#3b82f6' : e.faction==='RED' ? '#ef4444' : '#f59e0b';
        
        // 건물 강조
        if (e.type==='nexus') col = e.faction==='BLUE' ? '#34d399' : '#f87171';
        else if (e.type==='nexus_turret') col = e.faction==='BLUE' ? '#93c5fd' : '#fca5a5';
        else if (e.type==='tower') col = e.faction==='BLUE' ? '#60a5fa' : '#f87171';
        
        let r;
        if (e === player) { col = '#fcd34d'; r = 4; }
        else if (e.type === 'nexus') r = 6;
        else if (e.type === 'nexus_turret') r = 3.5;
        else if (e.type === 'tower') r = 3;
        else if (e.type === 'hero') r = 3;
        else if (e.type === 'jungle') r = 1.5;
        else r = 1.5; // minion
        
        mCtx.fillStyle = col;
        mCtx.beginPath();
        mCtx.arc(e.x * sx, e.y * sy, r, 0, Math.PI * 2);
        mCtx.fill();
    });
    
    // 시야 사각형 (기존 유지)
    mCtx.strokeStyle = 'rgba(255,255,255,0.6)';
    mCtx.lineWidth = 1;
    mCtx.strokeRect(
        camera.x*sx - (window.innerWidth/camera.zoom*sx)/2,
        camera.y*sy - (window.innerHeight/camera.zoom*sy)/2,
        window.innerWidth/camera.zoom*sx,
        window.innerHeight/camera.zoom*sy
    );
}
```

---

## 4. 타워/넥서스 방어 순서 + 내구도 강화

### 4-1. game.js — Building HP 대폭 강화

`Building` 생성자 수정:
```javascript
class Building extends Entity {
    constructor(x, y, faction, btype) {
        super(x, y, faction, btype);
        this.isBuilding = true;
        if (btype === 'nexus') {
            this.maxHp = 10000;  // 기존 6000 → 10000
            this.atk = 0; this.range = 0; this.radius = 50;
        } else if (btype === 'nexus_turret') {
            this.maxHp = 8000;   // 기존 3500 → 8000 (최종보스급)
            this.atk = 400;      // 기존 280 → 400
            this.aspd = 1.5;     // 기존 1.3 → 1.5
            this.range = 350; this.radius = 22;
        } else { // 일반 타워
            this.maxHp = 6000;   // 기존 4000 → 6000
            this.atk = 280;      // 기존 220 → 280
            this.aspd = 1.2;     // 기존 1.1 → 1.2
            this.range = 360; this.radius = 28;
        }
        this.hp = this.maxHp;
    }
```

### 4-2. game.js — 넥서스 보호막 (수호탑 파괴 전 넥서스 무적)

`applyRawDamage()` 최상단 (isDead 체크 바로 다음)에 추가:
```javascript
applyRawDamage(amount, attacker, triggerEffects = true) {
    if (this.isDead) return 0;
    
    // ★ 넥서스 보호막: 수호탑(nexus_turret)이 하나라도 살아있으면 넥서스 무적
    if (this.type === 'nexus') {
        const guardTurretsAlive = entities.filter(
            e => e.type === 'nexus_turret' && e.faction === this.faction && !e.isDead
        ).length;
        if (guardTurretsAlive > 0) {
            // 피해 흡수 시각 효과
            spawnParticles(this.x + rand(-30,30), this.y - 20, '#60a5fa', 3, 60, 0.3);
            addText(this.x, this.y - 60, '🛡️ 보호됨', '#60a5fa', 12);
            return 0;
        }
    }
    
    // 이하 기존 로직 동일
    this.lastAttackedTimer = REGEN_DELAY + 0.5;
    // ...
```

### 4-3. game.js — 일반 타워 초반 보호 강화

기존 5분(300초) → 3분(180초)으로 줄이되, 감소율을 75%로 상향:
```javascript
// Building.update() 내 타워 공격 부분:
let dmg = this.atk;
if (target.type === 'hero' && GS.time < 180) dmg = Math.floor(dmg * 0.3); // 초반 3분간 영웅에게 70% 감소
if (target.type === 'jungle') dmg = Math.floor(dmg * 0.4);
```

### 4-4. game.js — 미니언도 일반타워 통과 제한 (라인타워 보호)

미니언이 타워를 지나치지 않고 먼저 공격하도록 — 기존 미니언 타겟팅이 150 사거리 내 아무 적이나 공격. 타워 우선순위 조정:

`Minion.update()` 내 타겟팅 로직:
```javascript
// 기존:
entities.forEach(e => { if(e.faction!==this.faction&&!e.isDead){let d=dist(this,e);if(d<minD){minD=d;target=e;}} });

// 교체: 타워/미니언 > 영웅 순서로 우선순위 (미니언은 영웅보다 건물/미니언 먼저 공격)
let closestBuilding = null, closestMinion = null, closestHero = null;
let dB = 120, dM = 100, dH = 120;
entities.forEach(e => {
    if (e.faction === this.faction || e.isDead) return;
    const d = dist(this, e);
    if ((e.type==='tower'||e.type==='nexus_turret'||e.type==='nexus') && d<dB) { dB=d; closestBuilding=e; }
    else if (e.type==='minion' && d<dM) { dM=d; closestMinion=e; }
    else if (e.type==='hero' && d<dH) { dH=d; closestHero=e; }
});
target = closestBuilding || closestMinion || closestHero;
```

---

## 5. 레벨업 속도 개선

### 5-1. game.js — EXP 획득량 전반 상향

`onKill()` 내 EXP 값 수정:
```javascript
// 기존 → 교체
} else if(target.type==='minion'){
    this.gold+=60; this.gainExp(25);   // 기존 15 → 25 (+67%)
    
} else if(target.type==='jungle'){
    this.gold+=100; this.gainExp(60);  // 기존 40 → 60 (+50%)
    
} else if(target.type==='hero'){
    this.gold+=250; this.gainExp(80);  // 기존 50 → 80 (+60%)
    
} else if(target.type.startsWith('boss')){
    this.gold+=400; this.gainExp(150); // 기존 100 → 150 (+50%)
```

### 5-2. game.js — 최대EXP 스케일링 완화

`gainExp()` 내:
```javascript
// 기존:
this.maxExp = Math.floor(this.maxExp * 1.25);

// 교체:
this.maxExp = Math.floor(this.maxExp * 1.15); // 레벨업 요구치 증가율 낮춤 (더 빠른 레벨업)
```

### 5-3. game.js — 시간 경과 EXP 보너스 (초당 미세한 EXP)

`Hero.update()` 내 골드 자동획득 부분에 추가:
```javascript
if (!this.isDead) {
    this.gold += dt * 4;
    this.gainExp(dt * 0.5); // 초당 0.5 EXP (5분이면 +150 EXP 보너스)
}
```

---

## 6. 게임 종료 종합 통계 화면

### 6-1. game.js — 영웅 사망 횟수 추적

`Hero` 생성자에 추가:
```javascript
// 기존 constructor 내 마지막 줄들 근처에:
this.deaths = 0;
this.assists = 0; // 향후 확장용
```

`Hero.onDeath()` 내에 추가:
```javascript
onDeath(attacker) {
    this.deaths++;  // ← 추가
    this.respawnTimer = Math.min(5 + this.level * 0.8, 25);
    // ... 기존 코드
}
```

### 6-2. game.js — buildScoreboard() 함수 추가

`Building.onDeath()` 내 nexus 사망 처리 부분에서 호출:
```javascript
onDeath(attacker) {
    if (this.type === 'nexus') {
        GS.status = 'GAMEOVER';
        document.getElementById('gameOverScreen').classList.remove('hidden');
        let win = this.faction !== 'BLUE';
        document.getElementById('txtGameResult').textContent = win ? '🏆 VICTORY' : '💀 DEFEAT';
        document.getElementById('txtGameResult').style.color = win ? '#34d399' : '#f87171';
        buildScoreboard();  // ← 추가
    }
    spawnParticles(this.x, this.y, '#f59e0b', 30, 300, 1.5);
}
```

**`buildScoreboard()` 함수 신규 추가** (game.js 맨 아래 `selectHero('BERSERKER');` 전에 삽입):
```javascript
function buildScoreboard() {
    const area = document.getElementById('scoreboardArea');
    if (!area) return;

    // 모든 영웅 수집
    const allHeroes = entities.filter(e => e.type === 'hero');
    const blueTeam = allHeroes.filter(e => e.faction === 'BLUE').sort((a,b) => b.kills - a.kills);
    const redTeam  = allHeroes.filter(e => e.faction === 'RED').sort((a,b) => b.kills - a.kills);

    const renderRow = (h, isPlayer) => {
        const name = HERO_TMPL[h.heroKey]?.name || h.heroKey;
        const kda = `${h.kills} / ${h.deaths} / 0`;
        const dmg = h.totalDmg >= 1000 ? (h.totalDmg/1000).toFixed(1)+'k' : h.totalDmg;
        const lvl = h.level;
        const playerMark = isPlayer ? ' 👑' : '';
        const rowBg = isPlayer ? 'rgba(252,211,77,0.1)' : 'transparent';
        return `
            <tr style="background:${rowBg}; ${isPlayer?'border:1px solid rgba(252,211,77,0.3);':''}">
                <td class="py-1 px-2 text-left">
                    <span class="text-xs font-bold text-white">${name}${playerMark}</span>
                    <span class="text-[9px] text-slate-400 ml-1">Lv.${lvl}</span>
                </td>
                <td class="py-1 px-2 text-center font-mono text-xs font-bold text-white">${kda}</td>
                <td class="py-1 px-2 text-center font-mono text-xs text-amber-400">${dmg}</td>
            </tr>`;
    };

    const renderTeam = (team, color, label) => `
        <div class="mb-3">
            <div class="text-xs font-black mb-1 px-1" style="color:${color}">▣ ${label}</div>
            <table class="w-full border-collapse">
                <thead>
                    <tr class="border-b border-slate-700">
                        <th class="text-[9px] text-slate-400 text-left py-0.5 px-2">영웅</th>
                        <th class="text-[9px] text-slate-400 text-center py-0.5 px-2">K/D/A</th>
                        <th class="text-[9px] text-slate-400 text-center py-0.5 px-2">딜량</th>
                    </tr>
                </thead>
                <tbody>
                    ${team.map(h => renderRow(h, h === player)).join('')}
                </tbody>
            </table>
        </div>`;

    const gameMin = Math.floor(GS.time/60), gameSec = Math.floor(GS.time%60);
    area.innerHTML = `
        <div class="text-[10px] text-slate-400 mb-3 text-center">
            경기 시간: ${String(gameMin).padStart(2,'0')}:${String(gameSec).padStart(2,'0')}
            &nbsp;|&nbsp; 블루 ${GS.scoreBlue} : ${GS.scoreRed} 레드
        </div>
        ${renderTeam(blueTeam, '#34d399', '🔵 BLUE TEAM')}
        ${renderTeam(redTeam, '#f87171', '🔴 RED TEAM')}
    `;
}
```

### 6-3. index.html — 게임 오버 화면 확장 (스크롤 가능하게)

기존 `#gameOverScreen` 내부 div:
```html
<!-- 기존 -->
<div class="text-center flex flex-col items-center w-full max-w-lg bg-slate-900/90 border border-slate-800 rounded-3xl p-8 shadow-2xl">

<!-- 교체: 스크롤 가능 + 더 넓게 -->
<div class="text-center flex flex-col items-center w-full max-w-xl bg-slate-900/90 border border-slate-800 rounded-3xl p-6 shadow-2xl overflow-y-auto" style="max-height:85vh;">
```

`#scoreboardArea` 스타일 보완:
```html
<div id="scoreboardArea" class="w-full mb-4 text-left"></div>
```

---

## 📌 구현 시 주의사항

### 스킬 타입 표시 (txtSkill1Type, txtSkill2Type) 제거
기존 HTML에서 스킬 버튼 내 `<span id="txtSkill1Type">자동</span>` 등 — 새 디자인에는 없으므로 **`toggleAutoSkill()` 함수에서도 해당 업데이트 코드 제거**:
```javascript
window.toggleAutoSkill = () => {
    GS.autoSkill = !GS.autoSkill;
    const btn = document.getElementById('btnAutoSkill');
    const txt = document.getElementById('txtAutoSkill');
    if (txt) txt.textContent = GS.autoSkill ? 'AUTO' : 'MANUAL';
    if (btn) {
        btn.style.background = GS.autoSkill
            ? 'rgba(245,158,11,0.85)'
            : 'rgba(100,116,139,0.7)';
        btn.style.color = GS.autoSkill ? '#0f172a' : '#cbd5e1';
    }
};
```

### 모바일 자동 플랫폼 감지 — titleScreen 로드 시점에도 적용
현재 `autoDetectPlatform()`이 `startGame()`에서만 호출됨. 타이틀 화면에서도 보이도록:
```javascript
// game.js 맨 아래, selectHero('BERSERKER'); 바로 위에 추가:
document.addEventListener('DOMContentLoaded', () => {
    autoDetectPlatform();
});
// 또는 script 로드 후 즉시 (DOMContentLoaded 없이도 됨):
autoDetectPlatform();
```

### 아이템 상점 골드 표시 동기화
기존 `renderShop()` 내 `document.getElementById('hudGoldText').textContent` 는 그대로 유지 (골드 표시 div가 바뀌어도 id는 동일).

---

## 📋 Gemini 전달 프롬프트

```
아래는 운빨 아발론 game.js (1602줄) + index.html의 2차 개선 기획서입니다.
각 섹션의 지시에 따라 기존 파일을 수정하여 완전한 game.js와 index.html을 출력해주세요.

중요 우선순위:
1. BGM 코드 완전 제거 (BGM_URLS, bgmAudio, playNextBGM, bgmFallback iframe)
2. 모바일 HUD 재설계 (스킬바 배경 제거, 골드 분리 표시, 상점 슬라이드업)
3. playSFX를 플레이어 관련 이벤트에만 한정
4. 미니맵 gameTimerMini 업데이트 + 죽은 엔티티 필터
5. 넥서스 보호막 (nexus_turret 전부 파괴 전 넥서스 무적)
6. 건물 HP 강화 (tower:6000, nexus_turret:8000, nexus:10000)
7. EXP 획득량 전반 상향 + maxExp 스케일 1.15로 하향
8. 게임 종료 종합 통계 (deaths 추적 + buildScoreboard 함수)

[기획서 내용 전체 붙여넣기]
```

---
*생성일: 2026-06-08 | 기준 버전: 운빨 아발론 v5.0 (game.js 1602줄)*
